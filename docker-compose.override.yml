services:
  frontend:
    build:
      dockerfile: Dockerfile.dev
    volumes:
      - ./frontend/src:/app/src:ro,z
      - ./frontend/public:/app/public:ro,z
      - ./frontend/next.config.ts:/app/next.config.ts:ro,z
      - ./frontend/postcss.config.mjs:/app/postcss.config.mjs:ro,z
      - ./frontend/components.json:/app/components.json:ro,z
      - ./frontend/package.json:/app/package.json:ro,z
      - ./frontend/pnpm-lock.yaml:/app/pnpm-lock.yaml:ro,z
      - ./frontend/tsconfig.json:/app/tsconfig.json:ro,z
      - frontend_node_modules:/app/node_modules
    environment:
      - NODE_ENV=development
      - WATCHPACK_POLLING=true
    command: npm run dev

  backend:
    volumes:
      - ./backend/app:/app/app:ro,z
      - ./shared:/app/shared:ro,z
    environment:
      - RELOAD=true
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir /app/app --reload-dir /app/shared

volumes:
  frontend_node_modules:
